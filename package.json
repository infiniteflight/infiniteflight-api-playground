{
  "name": "@infiniteflight/api-sample-flight-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Map-first Infinite Flight OAuth-only PublicApi flight tracker sample.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "cf:dev": "npx wrangler pages dev public",
    "cf:deploy": "npx wrangler pages deploy public"
  },
  "engines": {
    "node": ">=20"
  }
}
